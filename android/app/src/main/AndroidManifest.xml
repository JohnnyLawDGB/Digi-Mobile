<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="Digi-Mobile"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigiMobile">
        <activity
            android:name="com.digimobile.app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.digimobile.app.NodeSetupActivity"
            android:exported="false" />

        <activity
            android:name="com.digimobile.app.CoreConsoleActivity"
            android:exported="false" />

        <activity
            android:name="com.digimobile.app.SetupWizardActivity"
            android:exported="false" />

        <activity
            android:name="com.digimobile.app.WalletActivity"
            android:exported="false" />

        <service
            android:name="com.digimobile.app.NodeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
