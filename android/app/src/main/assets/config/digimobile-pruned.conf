# Digi-Mobile default pruned configuration (asset)
# Paths are overridden at runtime to the app's private directories.
prune=550
server=1
daemon=0
listen=1
txindex=0
rpcallowip=127.0.0.1
rpcbind=127.0.0.1
